# Collector config for the local pipeline test. Paths are relative to test/rns,
# under .run (recreated each run). allowlist mode = open so the test monitor can
# write without an extra allowlist step.

[storage]
dir = .run/collector

[allowlist]
mode = open

[reticulum]
config_dir = .run/reticulum/collector
