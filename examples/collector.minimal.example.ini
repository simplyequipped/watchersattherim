# Minimal watchersattherim collector config. Everything defaults; the main thing
# to set is the monitor allowlist (or use `watr-collector --allow <hash>`).

[allowlist]
mode = explicit
# allowed =
#     <monitor-lxmf-address-hex>
