# Minimal watchersattherim monitor config - only the required keys.
# Everything omitted falls back to its default (see config.full.example.ini).

[monitor]
grid = FN19

[receiver:20m]
freq = 14074000
card = 8

[collector]
address = <collector-lxmf-address-hex>
